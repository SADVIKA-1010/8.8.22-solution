#include<stdio.h>
int main()
{
	int n,r=0,sum=0,t;
	printf("enter a number");
	scanf("%d",&n);
	t=n;
	while(n>0)
	{
		r=n%10;
		sum=sum+r*r*r;
		n=n/10;
	}
	if(t==sum)
	printf("amstrong number");
	else
	printf("not an amstrong number");
	return 0;
}
